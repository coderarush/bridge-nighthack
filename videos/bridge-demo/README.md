# Bridge Demo Video

A 48-second, silent Remotion walkthrough of the verified AtlasPay migration.

## Render

```bash
npm ci
npm run typecheck
npm run render
```

The video is written to `out/bridge-demo-silent.mp4`. The timed narration is in
`VOICEOVER.md`.
