{
  "name": "atlas-store-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Sample customer TypeScript integration for the Bridge NightHack demo (AtlasPay).",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "node --import tsx --test tests/**/*.test.ts"
  },
  "devDependencies": {
    "@types/node": "^22.9.0",
    "tsx": "^4.19.2",
    "typescript": "^5.6.3"
  }
}
